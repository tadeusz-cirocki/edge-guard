# edge-guard

Lightweight reverse proxy with basic security filtering (WAF-like behavior) written in Go.

## Features (planned)
- Reverse proxy
- Request inspection (SQLi, SSRF patterns)
- IP blocking
- Rate limiting
- Structured logging

## Goal
Educational project to understand how security controls can be implemented at the proxy layer.